syntax = "v1"

import "user.api"

info(
	title: "type title here"
	desc: "type desc here"
	author: "type author here"
	email: "type email here"
	version: "type version here"
)

type PingRes {
	Msg string `json:"msg"`
}

@server (
	group: test
)
service app {
	@doc "ping"
	@handler PingHandler
	get /ping returns (PingRes)
}